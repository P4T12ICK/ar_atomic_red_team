---

# A list of mitre att&ck technique ids to execute (runs all atomics for each technique)
ar_atomic_red_team_techniques: []

# A list of specific atomics to execute; each entry requires technique id and atomic test guid
# Example: [{technique: T1003.001, guid: 0be2230c-9ab3-4ac2-8826-3199b9a0ebf8}]
ar_atomic_red_team_atomics: []

# Custom atomic YAML files to deploy and execute on the target host.
# Each entry requires either path (on the Ansible controller) or content (inline YAML).
# Optional fields: technique (defaults to attack_technique in YAML), guid (defaults to the
# sole test's auto_generated_guid when the file contains one atomic test), src_dir (companion src/).
# Example: [{path: /path/to/T1003.001.yaml}, {content: "...", guid: "..."}]
ar_atomic_red_team_atomic_files: []

# Maximum seconds for atomic test execution before Invoke-AtomicTest aborts the test
ar_atomic_red_team_timeout_seconds: 60

# Extra seconds added to ar_atomic_red_team_timeout_seconds for the Ansible task timeout
# (kills the remote shell if Invoke-AtomicTest does not stop in time)
ar_atomic_red_team_ansible_timeout_buffer_seconds: 30
